# ROI tool for hyperspectral image

## Install and run the code :minidisc:
1. Clone the Repository
```Python
git clone https://github.com/DWEIEEE/ROI-tool-for-hyperspectral-image.git
```

2. Install Depedency
```Python
cd ROI-tool-for-hyperspectral-image
pip install -r requirements.txt
```

3. Open Application
```Python
python3 main_0.1.py
```
